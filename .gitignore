node_modules
dist
*.local
temp-app
.env
.env.*
!.env.example

# Wrangler local state (generated by wrangler dev/deploy at project root)
.wrangler/

# Build artifacts
*.tsbuildinfo
.vite/
*.zip

# Editor/IDE
# Cursor: keep .cursor/rules/ (project-level AI behavior) but ignore personal files
.cursor/debug-*.log
.cursor/settings.json
.cursor/plans/

# Claude Code local workspace (worktrees, session caches)
.claude/

# Documentation (kept locally only); allow public docs sources for contributors
docs/*
!docs/accessibility.md
!docs/nvalope-user-guide.md
!docs/versioning.md

# Deploy staging (local use only)
To Deploy/

# Internal / not for public repo
instructions.md
DEPLOY.md
AGENTS.md
*.zipfilelist.txt

# Test and coverage artifacts
coverage/
playwright-report/
test-results/

# Local scratch / reports
.jscpd-report/
CSVEXAMPLE.csv
CSVEXAMPLE.txt

# Auto-generated sidecar from scripts/context-pack.sh (the hand-curated
# NVALOPE_CONTEXT.md is the canonical artifact and stays tracked).
NVALOPE_CONTEXT.generated.md
