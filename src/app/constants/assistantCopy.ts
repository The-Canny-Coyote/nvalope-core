/**
 * User-facing copy for Cache the Coyote, AI Companion (shared where WebLLM / local LLM is relevant).
 */

/** Shown when the assistant uses the in-browser language model (not rule-based replies only). */
export const LOCAL_LLM_ACCURACY_NOTE =
  "When the local AI model is answering, it can sometimes be inaccurate or miss details. Double-check important amounts and dates against your budget in the app.";

/** Short line for module cards / section intros (full accuracy note is in Settings and the assistant sheet). */
export const CACHE_MODULE_LOCAL_AI_HINT =
  "On-device AI is optional in Settings or the assistant; verify important totals in the app.";
