/** UI/animation timing constants. Used by App and hooks to avoid magic numbers. */

/** Delay for cache-enable animation before adding module (ms). */
export const CACHE_ENABLE_ANIMATION_MS = 600;

/** Delay before restoring scroll position after layout change (ms). */
export const SCROLL_RESTORE_DELAY_MS = 100;
